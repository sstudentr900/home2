//取得public目錄下的完整位置
```php
    public_path('img\balogo.png')
```    

//產生隨機檔案名稱
```php
     uniqid()
```   


//圖片上傳
```php
    static function imgAdd( $image,$customImgName){
        $imgName =  $customImgName.date(time()).'.jpg';
        $path = 'img/'.$imgName;//圖片路徑
        file_put_contents($path, base64_decode(str_replace('data:image/jpeg;base64,','', $image)));
        return $imgName;
    }
    static function imgUpdata($image,$data,$customImgName){
        if($data->cover!=$image){
            if($image=='error.jpg'){
                return $this->imgAdd($image,$customImgName);
            }else{
                $path = 'img/'.$data->cover;//圖片路徑
                file_put_contents($path, base64_decode(str_replace('data:image/jpeg;base64,','', $image)));
                return '';
            }
        }
    }
    static function imgDelet($data){
        if($data->cover !='error.jpg'){
            $imgName = 'img/'.($data->cover);
            if(file_exists($imgName) && is_file($imgName)){
                unlink($imgName);
            }
        }
    }
    //imgAdd
    $cover = CustomFn::imgAdd($input['cover'],'filName');
    //imgUpdata
    $imgUpdata = CustomFn::imgUpdata($input['cover'],$data,'filName');
    if($imgUpdata){
        //data update
        $data->cover = $imgUpdata;
    }
    //imgDelet
    $cover = CustomFn::imgDelet($data);

```
